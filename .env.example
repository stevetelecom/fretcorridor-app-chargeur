# Copier ce fichier en ".env" et renseigner de vraies valeurs.
# ".env" ne doit JAMAIS etre commit (deja dans .gitignore).

DB_NAME=fretcorridor
DB_USER=fretcorridor_user
DB_PASSWORD=change_me_en_local

# Genere avec : openssl rand -base64 64
JWT_SECRET=change_me_avec_une_vraie_cle_secrete_longue

JWT_EXPIRATION_MS=3600000
JWT_REFRESH_EXPIRATION_MS=604800000
